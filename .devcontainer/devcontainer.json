{
  "name": "LilyPond Dev Container",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "mounts": [
    "source=${localWorkspaceFolderBasename}-node_modules,target=${containerWorkspaceFolder}/web/node_modules,type=volume"
  ],
  "features": {
    "ghcr.io/devcontainers/features/python:1": {},
    "ghcr.io/devcontainers/features/node:1": {
      "version": "24"
    },
    "./local-features/lilypond": {
      "version": "stable"
    }
  },
  "postCreateCommand": "python -m venv ~/.venv && ~/.venv/bin/pip install -r tools/requirements.txt && sudo chown -R vscode:vscode web/node_modules && npm --prefix web ci",
  "customizations": {
    "vscode": {
      "extensions": [
        "jeandeaual.lilypond-syntax",
        "mando.lilypond-pdf-preview"
      ]
    }
  }
}
